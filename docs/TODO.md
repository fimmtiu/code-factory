TO DO

- Some sort of `tickets clean` command that removes directories for all tickets/projects that are closed? Is it worth bothering? Probably not.

- '/' command to filter tree view or log view. 'Esc' clears. Uses ephemeral pop-ups.

- The editor workflow sucks for permission requests, where it just wants a Y/Y/N response. Make a really quick interface for approving those.

- Let's put a "Waiting for editor..." dialog in the center of the screen whenever a blocking editor starts up, so that users know why the UI is unresponsive. The dialog should close when the editor exits.
